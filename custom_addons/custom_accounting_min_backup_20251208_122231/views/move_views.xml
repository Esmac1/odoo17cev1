<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- FORM VIEW -->
    <record id="view_custom_move_form" model="ir.ui.view">
        <field name="name">custom_accounting.move.form</field>
        <field name="model">custom_accounting.move</field>
        <field name="arch" type="xml">
            <form string="Journal Entry">
                <header>
                    <!-- Buttons -->
                    <button name="action_post" type="object" string="Post"
                            class="btn-primary"
                            invisible="state != 'draft'"/>

                    <button name="action_cancel" type="object" string="Cancel"
                            class="btn-secondary"
                            invisible="state != 'posted'"/>

                    <button name="action_draft" type="object" string="Reset to Draft"
                            class="btn-light"
                            invisible="state != 'posted'"/>

                    <field name="state" widget="statusbar" statusbar_visible="draft,posted"/>
                </header>

                <sheet>
                    <group>
                        <group>
                            <field name="name" readonly="state != 'draft'"/>
                            <field name="date" readonly="state != 'draft'"/>
                            <field name="journal_id" readonly="state != 'draft'"/>
                            <field name="ref" readonly="state != 'draft'"/>
                            <field name="company_id" readonly="1"/>
                        </group>

                        <group>
                            <field name="amount_total" readonly="1"/>
                            <field name="balance_check" readonly="1"/>
                        </group>
                    </group>

                    <notebook>
                        <page string="Journal Items">
                            <field name="line_ids" readonly="state != 'draft'">
                                <tree editable="bottom">
                                    <field name="name"/>
                                    <field name="account_id"/>
                                    <field name="debit"/>
                                    <field name="credit"/>
                                </tree>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>



    <!-- TREE VIEW -->
    <record id="view_custom_move_tree" model="ir.ui.view">
        <field name="name">custom_accounting.move.tree</field>
        <field name="model">custom_accounting.move</field>
        <field name="arch" type="xml">
            <tree string="Journal Entries">
                <field name="name"/>
                <field name="date"/>
                <field name="ref"/>
                <field name="journal_id"/>
                <field name="amount_total"/>
                <field name="state"/>
            </tree>
        </field>
    </record>



    <!-- SEARCH VIEW -->
    <record id="view_custom_move_search" model="ir.ui.view">
        <field name="name">custom_accounting.move.search</field>
        <field name="model">custom_accounting.move</field>
        <field name="arch" type="xml">
            <search string="Search Journal Entries">
                <field name="name"/>
                <field name="ref"/>
                <field name="journal_id"/>

                <filter string="Draft" name="draft" domain="[('state','=','draft')]"/>
                <filter string="Posted" name="posted" domain="[('state','=','posted')]"/>
            </search>
        </field>
    </record>



    <!-- WINDOW ACTION -->
    <record id="action_custom_move" model="ir.actions.act_window">
        <field name="name">Journal Entries</field>
        <field name="res_model">custom_accounting.move</field>
        <field name="view_mode">tree,form</field>
    </record>

</odoo>
